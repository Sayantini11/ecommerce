<h2> Listing Categories (<%= @categories.length %>) </h2>
<ol>
      <% @categories.each do |category| %>
            <li> <%= category.name %> <%= link_to "Show", category_path(category.id) %> </li>
      <% end %>
</ol>
<%= link_to "Add Category",new_category_path %>